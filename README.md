# Das Hohe Haus (vormals GF Weekly)

Geschäftsführung der Wilden Habitate: internes Cockpit von Alex und Lea. Live: https://hohes-haus.netlify.app (Netlify-Projekt am 15.09.2026 von gfweekly auf hohes-haus umbenannt; Repo, Site-ID, Tabellen und Edge Function heißen weiter gfweekly).

## Aufbau
- `site/` – statische Seite, wird von Netlify veröffentlicht (Projekt „gfweekly", Site-ID e5a94704-6693-4558-abca-5be5f7e3ab22). Kein Build, Vanilla JS.
  - `index.html` Start (Jahreszyklus, wichtigste Themen, Rituale, Schnellzugriff)
  - `seiten.html` Wichtige Seiten (Seitenverzeichnis mit Zugangsdaten)
  - `cockpit.html` Themen-Cockpit · `checkin.html` · `capture.html` · `inbox.html` · `bearbeiten.html`
  - `assets/core.js` gemeinsamer Kern (API, Login-Gate, Navigation, Theme) · `assets/game.js` Habitat-Punkte · `assets/eggs.js` Easter Eggs · `assets/styles.css` Design
  - `_headers` Sicherheits-Header
- `supabase/functions/gfweekly/index.ts` – Edge Function (Supabase-Projekt bnfmupnmqyrcltrphfak). Enthält das Zugangspasswort, gehört deshalb nie in `site/`.
- `netlify.toml` – veröffentlicht nur `site/`.

## Design
Seit V9 (12.09.2026) nach dem Design System „Wilde Habitate" (Claude Design d8550ead-911c-4134-8036-38fc87ae4b99; Quelle: `festivalplanung-2027/docs/spiel/design-export/tokens`). Tokens stehen am Anfang von `site/assets/styles.css`; die bisherigen GF-Weekly-Namen (`--ink`, `--brand`, `--line` …) sind darauf gebrückt. Dunkel ist Standard, Hell über `[data-theme="light"]`. Regeln: Archivo, fünf Schriftstufen, Abstände 4/8/12/16/24/32/48, Schatten nur bei angehobenen Karten, keine Pillen, keine Farb-Emoji.

V9.1 (13.09.2026) legt die Optik des Design Systems „Modernist" (Claude Design 9f0f400a-5e4c-4bd9-9fad-875538d709d2) darüber — als reine Token-Änderung, keine Komponentenregel wurde angefasst:

- Radien **0** statt 4/6/8 (`--radius-s/m/l`). Kreise bleiben rund: Fortschrittsring und LED-Punkte sind Kreise, keine gerundeten Ecken.
- Abschnittslinien **2 px** statt 1 px (`--rahmen`) — greift auf Topbar, Protokollleiste und Abschnittsköpfe.
- Bauteilrahmen in `--border-strong` statt `--border` (`--line`), damit die Struktur sichtbar wird, ohne jedes Bauteil auf 2 px aufzublasen.
- Beschriftungen bündig links, auch im Login-Gate.

Farbrollen, Dunkelmodus und Statusfarben bleiben unverändert Habitate. Modernists roter Akzent (#ec3013) wurde **bewusst nicht** übernommen: er kollidiert mit `--crit` („ausgefallen"), das im Cockpit dieselbe Farbe als Signal trägt. Die vier Diagrammfarben `--chart-teal/-peach/-coral/-grid` sind aus der Habitate-Quelle nachgezogen und stehen für spätere Kennzahl-Visualisierungen bereit.

## Deploy
Repo: <https://github.com/AlexanderDettke/gfweekly> (privat — `supabase/functions/gfweekly/index.ts` enthält das Zugangspasswort und gehört nie in `site/`).

**Continuous Deployment ist eingerichtet** (13.09.2026). Netlify baut aus dem Git-Repo: Branch `main`, Build command leer, Publish directory `site` (aus `netlify.toml`). Jeder Push auf `main` deployt automatisch; ein Lauf dauert rund eine Minute. Kontrolle im Deploy-Datensatz über `commit_ref` und `branch` — Git-Deploys tragen den Commit-Hash, manuelle Uploads nicht.

Manuell deployen (Notfall, oder wenn CD einmal klemmt): Der Netlify-MCP-Connector (`deploy-site`) deployt nicht selbst, sondern liefert einen fertigen `npx @netlify/mcp`-Befehl mit eingebettetem Auth-Token. Der läuft **nicht-interaktiv** — anders als `netlify login` und `netlify init`, die auf Browser-Klicks warten. Voraussetzung ist Node, das auf dem Arbeitsrechner fehlt; ein Tarball von nodejs.org nach `~/` oder in ein temporäres Verzeichnis genügt (kein `sudo`). Ohne jedes Werkzeug geht auch ein Netlify-Drop des Ordners `site/`.

Die Seite wird **ohne Zugangskontrolle ausgeliefert** (`requiresPassword: false`): HTML, CSS und `core.js` kann jeder abrufen, der die URL kennt — `noindex` hält nur Suchmaschinen fern. Die Daten liegen dagegen vollständig hinter dem Passwort der Edge Function. Wer auch das Frontend verbergen will, kann auf dem Pro-Plan Netlifys Passwortschutz auf Seitenebene aktivieren (Project configuration → Visitor access); dann sind es zwei Passwörter hintereinander.

## Meta-Planung Stufe 2 (V10, 13.09.2026)
Startseite liest Zyklus, Rituale und Meilensteine aus Supabase (`gfweekly_cycle_phases`, `gfweekly_cycle_transitions`, `gfweekly_rituals`, `gfweekly_ritual_checks`, `gfweekly_strands`, `gfweekly_milestones`). Rituale sind je Jahr abhakbar, Meilensteine in der Seite pflegbar und auf der Jahresleiste sichtbar (Jahr umschaltbar). Edge-Function-Actions: `cycle_get`, `ritual_toggle`, `ritual_save`, `ritual_delete`, `milestones_list`, `milestone_save`, `milestone_delete`.

## V11 (13.09.2026): Board, Bildwelt, Lebendigkeit
`site/board.html` = Themen als Board oder Kacheln, Spalten nach Ablauf (Zu besprechen · In Klärung · Entschieden · Erledigt), Prio, Zeitraum oder Person; Verschieben per Drag & Drop ändert das jeweils passende Feld (`board_lane`+`status`, `priority`, `next_suggested`, `owner`), 5 s Rückgängig; Detail-Panel rechts. `cockpit.html` bleibt die Liste (Unternavigation Board · Kacheln · Liste). Die Inbox ist kein Zwischenschritt mehr: `capture`/`capture_many` legen direkt Themen in „Zu besprechen" an, alte Inbox-Einträge wurden migriert, `inbox.html` ist nur noch Archiv ohne Navigationseintrag. Sechs Low-Poly-Grafiken in `site/assets/img/` (WebP, ≤ 120 kB): Tor (Login), Ankunft (Start), Kiste im Regen (Eingabe), Lager (Check-in), Bühne im Herbst (Wichtige Seiten), Lagerfeuer (Themen). Lange Texte klappen ab ~220 Zeichen ein (`gfClamp`). Bewegung nach Design System (ht-rise, 120/200/400 ms, prefers-reduced-motion).

## V12 (14.09.2026): Kopfzeile, Bilder, Wichtige Seiten
Kopfzeile in zwei festen Zeilen (Marke + Dunkel/Hell + Person „Alex | Lea“; darunter Navigation als Tab-Leiste, rechts die Unternavigation der Themen). Grafiken stehen frei im Bild (2,4:1 bzw. 3:1), ohne Scrim und ohne Text darauf; Titelzeile darunter. Login-Tor: Bild links, Karte rechts. Wichtige Seiten mit Vorschaubildern (`site/assets/previews/*.webp`, Pfad in `gfweekly_sites.preview`) und drei Ansichten: Kacheln, Liste, Schnellstart. Assets werden versioniert geladen (`?v=13`), HTML/CSS/JS mit `must-revalidate`; bei jeder Änderung an core.js/styles.css die Nummer in allen HTML-Köpfen hochzählen.

## V13 (14.09.2026): Besprechungsmodus, Entscheidungslog, Wochenmail
Im Board „Besprechung starten“: Sitzung mit Timer und Teilnehmenden, Agenda aus „Zu besprechen“ und „In Klärung“ (hoch zuerst), je Thema Entscheidung, nächster Schritt und Verantwortung erfassen und mit einem Klick als Entschieden, In Klärung, Erledigt, Vertagt oder nur besprochen ablegen. Jede Entscheidung landet im Log (`gfweekly_decisions`), die Sitzung mit Protokolltext in `gfweekly_sessions`. „Beenden · Protokoll“ zeigt das Protokoll zum Kopieren oder als Mail-Entwurf. Neue Seite `site/entscheidungen.html` (Unternavigation der Themen): Entscheidungslog nach Monaten, nachtragbar und editierbar; Protokolle der Besprechungen; „Wochenmail erzeugen“ baut einen Entwurf aus Entscheidungen der Woche, offenen Prioritäten, Meilensteinen und nächstem Übergang. Karten haben einen ⇄-Knopf zum Verschieben per Menü (Touch, Tastatur). Edge-Function-Actions: `session_start`, `session_end`, `sessions_list`, `session_delete`, `decision_add`, `decision_update`, `decisions_list`, `decision_delete`.

## V14 (14.09.2026): Easter Eggs
`site/assets/eggs.js` (auf allen Seiten geladen, ohne Ton, respektiert prefers-reduced-motion): fünfmal schnell auf die Fahne klicken → Konfetti in Habitate-Farben, der Besprechungs-Timer zeigt kurz „Pause?“; leere Agenda in einer Besprechung → ein Bus fährt unten über die Seite („tuut“); zwischen 23 und 5 Uhr funkeln Sterne über dem Login-Tor, Untertitel „eigentlich Feierabend“; irgendwo „möhre“ tippen → die Fahne wird kurz zur Möhre, Kennzahlen zählen runter und wieder hoch; alle Rituale der aktuellen Phase abgehakt → Abzeichen „Phase gemeistert“ mit flackerndem Feuer bei Hover; 20. bis 23. August → Wimpelkette an der Fahne, Begrüßung „Heute ist Möhre.“ Zum Testen: `?egg=fest|nacht|bus|konfetti|moehre`.

## V15 (14.09.2026): Verlässlich speichern, Bereinigen, Check-in als Vorbereitung
Detail-Panel im Board: Felder speichern sich selbst (1,5 s nach dem Tippen, beim Verlassen eines Feldes, beim Schließen des Panels, beim Wechsel des Tabs), Status „Gespeichert 13:05“ im Fuß. Spaltenknöpfe und „heute besprochen“ nehmen getippte Texte mit, statt das Panel neu zu zeichnen (das war die Ursache für „Speichern gedrückt, trotzdem weg“). Panel-Höhe iPhone-sicher (`100dvh`, Safe Area). „Bereinigen“ im Panel und „Aufräumen“ in der Board-Leiste: `gfTidyTopic` (core.js) erkennt Marker im Titel und Kontext („Titel:“, „Worum geht es:“, „Warum ins Weekly:“, „Stand:“, „Vorschlag:“, „Nächster Schritt:“, „Verantwortlich:“, „Entscheidung:“, „Quelle:“ …) und schlägt eine Aufteilung in Titel, Ein Satz, Kontext, Nächster Schritt, Verantwortung, Entscheidung und Notizen vor; Vorher/Nachher je Feld, rechts editierbar, „Übernehmen“ speichert. „KI-Vorschlag“ lässt Claude den Text aufteilen (Action `tidy_suggest`, Secret `ANTHROPIC_API_KEY`, Modell über `GFWEEKLY_TIDY_MODEL`, Standard `claude-sonnet-5`). Die Edge Function (v20) zerlegt neue Eingaben aus dem ChatGPT-Kanal (`capture`, `capture_many`) sofort mit derselben Logik; der Rohtext bleibt in den Notizen. `checkin.html` ist jetzt die Vorbereitung auf das Weekly: Agenda in der Reihenfolge des Besprechungsmodus, Neu seit der letzten Besprechung, Termine der Woche und Überfälliges, länger als vier Wochen Offenes, Entschiedenes mit laufendem nächstem Schritt, letzte Besprechung, „Besprechung starten“, „Agenda kopieren“. Die alten Felder Relevanz und Empfehlung spielen dort keine Rolle mehr.

## V16 (14.09.2026): Neuigkeiten
Neue Seite `site/neuigkeiten.html` (Hauptnavigation, zwischen Start und Themen; Badge = Kandidaten im Sichtungskorb). Drei Ebenen aus einer Tabelle `gfweekly_news`: **Ticker** (eine Zeile je Ereignis aus Besprechungsnotizen, Mails, Asana und Kalender, nach Tagen gruppiert, „Demnächst“ für kommende Termine, Klick öffnet Details und Quelle, „Als Thema erfassen“), **Sichtungskorb** (Kandidaten mit Relevanz hoch/mittel/niedrig, Zitat und Quelle; „Als Thema übernehmen“ legt ein Thema in „Zu besprechen“ an, „An Thema anhängen“/„Mit Thema verknüpfen“ notiert den Hinweis am bestehenden Thema, „Verwerfen“), **Themenlage** (je Strang ein Absatz Stand, `kind=lage`, wird vom täglichen Lauf ersetzt). Filter: Person (Beide/Alex/Lea), Zeitraum, Quellen, Suche; „Alles gesehen“ merkt sich je Person den Stand in localStorage (`gf_news_seen_<Person>`), Neues bekommt einen Punkt. Neuer Strang `fluidity` in `gfweekly_strands`.
Befüllt wird die Tabelle vom täglichen Cowork-Auftrag (7 Uhr): Gemini-Notizen aus Drive („Notizen von Gemini“, inkl. der von Lea geteilten), Asana (nur Projekte der Gesellschaft, keine privaten Listen), Kalender Alex und Lea (nur Termine mit Teilnehmenden oder klarem Arbeitsbezug, keine Gesundheits-/Privattermine), Mail-Betreffe und Absender aus Alex' Postfach (Systemmails und Newsletter gefiltert). Erstbefüllung am 14.09. mit allen Notizen seit 24.07. (`run_id=seed-20260914`). Dedup über `source_ref` (`drive:<fileId>`, `drive:<fileId>:k1`, `mail:<threadId>`, `asana:…`, `cal:…`, `lage:<strand>`).
Edge Function v21: `news_add_many` (Upsert; Ticker/Kandidaten werden bei gleicher `source_ref` übersprungen, Themenlage ersetzt), `news_list` (kinds, statuses, since, strand, limit), `news_update` (Felder, Status mit decided_by/decided_at), `news_accept` (Thema anlegen oder Hinweis an `topic_id` hängen, Kandidat → übernommen), `news_delete`. Migration `supabase/migrations/20260914_gfweekly_news.sql`.

## V17 (14.09.2026): Habitat-Punkte (Spielsystem)
Konzept: Projektdoku „Plan V16 Habitat-Punkte“. Eine Währung (Habitat-Taler), jeder Taler wird einer Person zugeschrieben und zählt zugleich in den gemeinsamen Jahrestopf, aus dem das Habitat in fünf Stufen wächst (Lichtung 0, Lagerplatz 600, Dorf 1.800, Festival 3.600, Habitat 6.000). Keine Abzüge, kein Verfall, keine Rangliste Alex gegen Lea.
Vergabe ausschließlich serverseitig in der Edge Function (v22) innerhalb der bestehenden Actions: `add`/`capture`/`capture_many` (Thema eingebracht 3, vollständig 5, höchstens fünf am Tag), `update` (erledigt 10, Altlast über 90 Tage +15, Verantwortung und nächster Schritt erstmals gesetzt 3, Saat an die einbringende Person 10 wenn entschieden oder erledigt), `ritual_toggle` (15, alle Rituale der Phase 100), `milestone_save` (erreicht 50), `session_end` (Besprechung 20 plus 5 je behandeltem Thema, an alle Teilnehmenden), `decision_add` (25). Neue Actions: `checkin` (5 je Person und Tag, gemessen am lokalen Tag des Browsers; Serie zählt Werktage in Folge, Wochenende unterbricht nicht; Serienbonus 3/7/14/30 Tage = 15/40/80/150), `score_get` (Stand: Topf, Stufe, Woche, Rang Bronze 150/Silber 350/Gold 600, Serie, Lagerfeuer-Serie, Tagesziel, Wochenziel, Abzeichen), `score_event` (nur `wochenmail`, `protokoll`, je 5, einmal je Woche bzw. Betreff), `score_events`, `score_rules`. Antworten aller Actions tragen `gains` (Liste der Gewinne). Tagesziel (10) rotiert aus fünf Aufgaben, Wochenziel (40) = Besprechung abgeschlossen und zwei Entscheidungen. 19 Abzeichen, einmal je Jahr, je 25 Taler.
Tabellen (Migration `supabase/migrations/20260914_gfweekly_habitat_punkte.sql`): `gfweekly_score_rules` (Werte und Tagesdeckel, ohne Code änderbar), `gfweekly_score_levels`, `gfweekly_score_events` (Ereignisprotokoll, Summen werden daraus gebildet; eindeutig über kind+ref_id+who), `gfweekly_checkins`, `gfweekly_badges`.
Frontend: `site/assets/game.js` (eigene Datei, wickelt `gfApi` ein: schickt `who` und `local_day` mit, wertet `gains` und `state` aus). Einchecken beim ersten Öffnen nach dem Entsperren, Tageskarte (Gruß, Serie, Serienbonus, Tages- und Wochenziel), Taler-Zähler mit Stufe und Fortschritt plus Serienflamme in der Kopfzeile, aufsteigende Marker „+N“ rechts oben bei jedem Gewinn, Abzeichenkarte, Stufenkarte mit Konfetti. Startseite: Kopfbild = Habitat-Stufe (habitat-1 bis -5), vier Karten Habitat/Heute/Woche/Abzeichen, erledigte Rituale mit Schatzbuch-Stempel. Board: Spaltenköpfe mit Bild (Ablauf, Prio, Lea-Porträt), Leerzustand mit Bild. Entscheidungen: Kopfbild, Abzeichenleiste (alle 19, erreichte in Farbe), Wochenmail nennt Rang und Taler und zählt 5. Protokoll per Mail zählt 5 (`gfTextModal`). Bilder: `site/assets/game/*.webp` (70 Dateien aus Asset-Brief 9, 192 px Icons, 1600 px Szenen); fehlende Motive werden in `game.js` (`FALLBACK`) durch vorhandene ersetzt. Testparameter `?game=karte|abzeichen|stufe|marker`. Assets `?v=17`.
Noch offen aus Brief 9: portraet-alex, abzeichen-drei-tage/-volle-woche/-ein-monat/-saatgut/-gaertner/-vollstaendig/-tagesziel-serie, punkte-einchecken/-thema-neu/-saat/-bewegen/-tagesziel/-wochenziel, tag-morgen/-mittag/-abend, serie-tage, serie-glut. Später (V16.3 im Plan): Chronik der Jahre mit Jahresabschluss, Belohnungskarte am Ende der Besprechung, Zwischenstand im Besprechungsmodus.

## V18 (15.09.2026): Das Hohe Haus, Seitenleiste links, Kopfbild je Seite
Umbenennung: „GF Weekly“ heißt jetzt „Das Hohe Haus“, Unterzeile „Geschäftsführung der Wilden Habitate“, Startseiten-Motto „Große Fragen. Klare Entscheidungen. Gelegentlich Kaffee oder besser Wein?“ (Konstanten `GF_APP_NAME`, `GF_APP_SUB`, `GF_APP_MOTTO` in core.js; Seitentitel, Login-Karten, Manifest, Protokoll- und Wochenmail-Fußzeilen angepasst). Technische Schlüssel (Repo, Netlify-Projekt, Tabellen `gfweekly_*`, Edge Function `gfweekly`) unverändert.
Navigation: statt der zweizeiligen Kopfleiste eine Seitenleiste links (`gfMountTopbar` in core.js baut sie in `#topbar`, das per `display:contents` nur noch Container ist). Drei Gruppen: Heute (Start, Neuigkeiten mit Sichtungskorb-Zähler, Check-in), Arbeiten (Themen mit eingerückten Unterpunkten Board · Kacheln · Liste · Entscheidungen und Zähler „zu besprechen“, Eingabe), Verwalten (Wichtige Seiten, Bearbeiten). Unten Habitat-Taler und Serie (game.js hängt sich weiter an `.tb-ctl`), Dunkel/Hell, Person Alex | Lea. Drei Zustände über `html[data-nav]`: `open` (264 px, Standard ab 1280 px), `rail` (68 px Symbolleiste, Standard 1024 bis 1279 px, Zähler als kleine Marke am Symbol), `drawer` (unter 1024 px: schmale Kopfzeile mit Menüknopf, Titel, Talern und Person; Leiste als Schublade mit Backdrop, Escape und Schließen-Knopf). Ein-/Ausklappen über den Pfeil im Kopf, Zustand in localStorage `gf_nav`; ein Inline-Skript im `<head>` setzt `data-nav` vor dem ersten Rendern (kein Sprung). `gfMountSubnav(active)` markiert nur noch den aktiven Unterpunkt; das alte `#subnav`-Element bleibt leer.
Kopfbilder (jede Seite ein eigenes): Start = Habitat-Stufe (dynamisch), Neuigkeiten = ankunft, Board = lagerfeuer, Kacheln = karten (phase-planung-szene), Liste = aufbau (phase-produktion-szene, neu mit Kopfbild), Entscheidungen = entscheidungen-kopf, Check-in = lager-begruessung, Wichtige Seiten = buehne-herbst, Eingabe = kiste-regen, Bearbeiten = pflege (phase-verbesserung-szene, neu mit Kopfbild). Wochenmail-Modal zeigt wochenmail-kopf (`gfTextModal` kennt `opts.pic`). Neue Schlüssel in `GF_IMG`/`GF_IMG_ALT`.
Behoben: eggs.js (Nachtmodus) schrieb den Login-Untertitel bei jedem Mutation-Event neu und geriet dadurch mit dem eigenen MutationObserver in eine Endlosschleife, sobald der Untertitel den Suchtext nicht enthielt; jetzt nur Zuweisung bei Änderung. Assets `?v=18`. Edge Function unverändert (v22).

### V18.1 (15.09.2026): Domain, Marke, Favicon, Avatare
Netlify-Projekt umbenannt: https://hohes-haus.netlify.app (alte Adresse gfweekly.netlify.app ist damit frei und antwortet nicht mehr). Fußzeilen, Startseiten-Filter und der Eintrag unter Wichtige Seiten (gfweekly_sites, jetzt „Das Hohe Haus“) zeigen auf die neue Adresse. Marke in Seitenleiste und Login-Karte ist die Strohburg (site/assets/img/burg-144.png, burg-288.png; die Fahne bleibt als Datei liegen). Favicon und App-Icons sind der Richterhammer (favicon-32/512, favicon.ico, icon-180 mit dunklem Grund, icon-192). Avatare von Alex und Lea (site/assets/img/avatar-alex.webp, avatar-lea.webp, rund 256 px; `GF_AVATAR`, `gfAvatarTag(who, cls)` in core.js) erscheinen im Personenschalter Alex | Lea (im schmalen Modus und in der mobilen Kopfzeile nur der Avatar), in der Begrüßung der Startseite, in der Heute-Karte und im Personenfilter der Neuigkeiten. Dazu portraet-alex.webp und portraet-lea.webp unter site/assets/game/ (portraet-alex war in Brief 9 offen).

## Backend
Tabellen `gfweekly_topics`, `gfweekly_inbox`, `gfweekly_people`, `gfweekly_links`, `gfweekly_protocol_requests`, `gfweekly_assets`, `gfweekly_sites`, `gfweekly_site_categories`, `gfweekly_sessions`, `gfweekly_decisions`, `gfweekly_news`, `gfweekly_score_rules`, `gfweekly_score_levels`, `gfweekly_score_events`, `gfweekly_checkins`, `gfweekly_badges`, dazu die Meta-Planungs-Tabellen aus V10.
